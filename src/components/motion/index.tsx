import { Card, CardHeader, CardContent } from '@/components/motion/card';
import Button from '@/components/motion/button';
import Link from '@/components/motion/link';

const Motion = {
  Card,
  CardHeader,
  CardContent,
  Button,
  Link,
};

export default Motion;
